


extends layout
 
block content
  

 style.
     @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@600&display=swap');

     h1, h3, p , a, button{
    
    font-family: 'Nunito', sans-serif; 

     } 

      #content{padding-bottom:200px;}
     

      #map{
     height:300px;
      } 

 script(src="https://maps.googleapis.com/maps/api/js?key=" + keyapi + "&libraries=places&callback=initMap" async defer)
 

 script.
   
    let map;
    var lat =  parseFloat('#{org.latitud}');
    var long = parseFloat('#{org.longitud}');
 
   function initMap() {
     map = new google.maps.Map(document.getElementById("map"), {
    center: { lat: lat,  lng: long },
    zoom: 18,

    });
  
    var marker =    new google.maps.Marker({
      position: {lat: lat , lng: long  },
      map,
     title: "Hello World!",
   });




  }  

  

  



 div.ui.doubling.stackable.container
   div.ui.segment
    div.ui.three.column.doubing.stackable.grid 
     div.column
      h2.textcenter  #{org.nombre} 
      h3.textcenter  #{org.pais} 🌍 
      h3.textcenter Dirección 📍
      p.textcenter  #{org.calle} # #{org.num} 
      p.textcenter #{org.estado}, #{org.ciudad}
      h3.textcenter Teléfono  📞
      p.textcenter  #{org.telefono}
     div.column

     div.column
      div#map

   div.ui.container
    div.ui.segment 
     h2.textcenter Descripción 📃
     p.textcenter  #{org.description}   	